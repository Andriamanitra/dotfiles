[init]
	defaultBranch = main
[pull]
	ff = only
[core]
	excludesfile = ~/.config/git/gitignore
[alias]
	undo = reset HEAD^
	unstage = reset HEAD --
	last = log -1 --stat HEAD
	st = status --short --branch
	pr = !sh -c 'git fetch $1 pull/$2/head:PR/$2 && git switch PR/$2' -
